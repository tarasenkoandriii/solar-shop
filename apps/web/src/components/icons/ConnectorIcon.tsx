import type { SVGProps } from "react";

// Категорія CONNECTOR — пара роз'ємів MC4 (саме комплект «мама+тато»,
// як воно й продається). Малюємо їх РОЗ'ЄДНАНИМИ, з видимим штирем і
// гніздом: з'єднана пара виглядала б просто як циліндр і не читалась
// би з 40 пікселів у сітці категорій.
//
// Фіксатори-«вушка» на корпусі були в першому варіанті й на рендері
// перетворились на два піксельні виступи-шум — прибрані. Замість них
// різниця між половинками показана чесно: зліва штир, справа гніздо.
export function ConnectorIcon(props: SVGProps<SVGSVGElement>) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.75}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      {...props}
    >
      {/* Провід і корпус лівої половини */}
      <path d="M1.4 12h2.2" />
      <rect x="3.6" y="8.2" width="5.6" height="7.6" rx="1.8" />
      {/* Штир */}
      <path d="M9.2 12h2.6" />
      {/* Корпус правої половини з гніздом під штир */}
      <rect x="13.6" y="8.2" width="5.6" height="7.6" rx="1.8" />
      <path d="M13.6 12h2.2" />
      {/* Провід праворуч */}
      <path d="M19.2 12h2.2" />
    </svg>
  );
}
