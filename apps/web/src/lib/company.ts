// Реквізити компанії — єдине джерело для всього apps/web.
//
// За запитом користувача (27.08.2026) підставлено реальні дані замість
// плейсхолдерів, які доти були в чотирьох різних місцях:
// payment/page.tsx, privacy/page.tsx, offer/page.tsx і Footer.tsx. Саме
// розкиданість і була проблемою: аудит показав, що покупцю відправлявся
// PDF-рахунок із рядком "ФОП [ПІБ] (заповнити перед запуском)" і
// "IBAN: [IBAN]" — заплатити за таким документом фізично неможливо.
//
// УВАГА: у apps/api є свій такий самий файл (src/common/company.ts) —
// застосунки не мають спільного пакета для констант, а заводити його
// заради п'яти рядків надлишково. Змінюєш тут — зміни і там.
//
// Окремо: це ТОВ, а не ФОП. Стара заглушка всюди казала "ФОП Тарасенко
// Андрій Євгенійович" — юридично інша форма, і в договорі оферти це не
// косметика.
export const COMPANY = {
  legalName: 'ТОВ «ХЕЙСЛОР ФАСТ»',
  edrpou: '44697367',
  address: 'Україна, 03110, м. Київ, пров. Караїмський, буд. 12',
  phone: '+380 75 365 75 86',
  email: 'heyslor@ukr.net',

  // Банківські реквізити користувач свідомо не надав ("остальные
  // реквизиты не нужны"). Порожній рядок, а НЕ вигаданий номер і не
  // плейсхолдер: усі місця нижче перевіряють значення й просто не
  // показують рядок, якщо він порожній. Щойно з'явиться IBAN — вписати
  // сюди, і він сам проступить на сторінці оплати та в рахунку.
  iban: '',
  bank: '',

  // Єдиний офіс — Київ. Години роботи були в таблиці Office разом із
  // трьома вигаданими демо-філіями (Харків, Одеса, Львів із адресами на
  // кшталт "Хрещатик, 1"); сторінка контактів більше з тієї таблиці не
  // читає, тож значення переїхало сюди.
  workHours: 'Пн–Пт 9:00–18:00',

  // Координати для карти на сторінці контактів. Не вигадані й не взяті
  // "приблизно по центру району": знайдені геокодером OpenStreetMap саме
  // за цією адресою — відповідь містила будинок 12 по Караїмському
  // провулку з індексом 03110, що збігається з address вище.
  //
  // Якщо офіс переїде: відкрий openstreetmap.org, клацни правою на
  // потрібній точці → "Показати адресу", і встав звідти пару чисел.
  // Порожні значення — карта просто не показується, замість того щоб
  // ставити мітку не там, де ви насправді є.
  lat: 50.4168785,
  lng: 30.4808074,
} as const;

// Телефон у форматі для tel: — без пробілів, які туди не можна.
export const COMPANY_PHONE_HREF = `tel:${COMPANY.phone.replace(/\s/g, '')}`;
