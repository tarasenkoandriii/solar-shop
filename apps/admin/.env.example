# Змінні середовища для apps/admin зведені в ЄДИНИЙ файл у корені монорепо —
# див. /.env.example (не тут). Причина і механізм підвантаження (кожен
# застосунок сам читає кореневий файл) — коментар на початку того файлу.
#
# Швидкий старт: скопіюй корінний /.env.example у /.env і заповни значення —
# цей файл підхопиться автоматично всіма 4 застосунками.
