{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && npm install && npm run db:generate",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
