{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && npm install && npm run db:generate && npm run db:build",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
